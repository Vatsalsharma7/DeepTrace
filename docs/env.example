# Copy this file to .env at the project root and adjust values

# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/forensiq

# Optional: database name (if not included in URI)
# MONGODB_DB=forensiq

# Backend port
PORT=5000

# Optional: customize database name separate from URI
# MONGODB_DB=forensiq

# Frontend (Vite) example
# Copy to client/.env and adjust if needed
# VITE_API_URL=http://localhost:5000/api


